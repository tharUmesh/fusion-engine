# Core inference dependencies
torch>=2.0
numpy>=1.24

# Only needed for skeleton extraction from camera frames
# (skeleton_utils.py + example_webcam.py). Pinned: other MediaPipe
# versions have produced incompatible landmark output in the past.
mediapipe==0.10.14
opencv-python>=4.8
